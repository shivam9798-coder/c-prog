#include <stdio.h>

int main () {
    int n,i=1,sum=0;
    printf("enter range");
    scanf("%d",&n);
    while(i<=n){
        sum=sum+i*i*i;
        i=i+2;
    }
    printf("sum = %d",sum);
    return 0;
}
